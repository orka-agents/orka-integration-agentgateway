# Contributing

Keep every upstream version pinned and isolate CRD churn in a versioned overlay. Use synthetic credentials and mock token endpoints in CI. Run `bash -n scripts/*.sh`, `scripts/check-redaction.sh`, and server-side manifest validation before submitting changes.
